    // Функция должна вставлять элемент what в начало элемента where
    // @param {Element} what - что вставлять
    // @param {Element} where - куда вставлять

function func(what, where) {
    where.prepend(what);
}
